#include <iostream>
#include <iostream>
#include <unordered_map>
#include <map>
#include <list>
#include <string>
#include <string.h>


using namespace std;


class Calculator {
public:    
	void calculate(int x,int y,char z) 
	{
		arithmetic.insert(pair< string, char>("multiply", 'm'));
		arithmetic.insert(pair< string, char>("divide", 'd'));
		arithmetic.insert(pair< string, char>("add", 'a'));
		arithmetic.insert(pair< string, char>("subtract", 's'));
		for (auto pair : arithmetic) {

			if (arithmetic[1] == z);
			{
				
			}
			cout << endl;
		}

	}

private:   

	map <string, char> arithmetic;
	
	double y=0;   
	double x=0;
	char z = ' ';
};



int main() {
	char n = ' ';
	double first = 0;
	double second = 0;

	Calculator first;

	 cout << "Enter multiply (m), divide (d), add (a) or subtract (s): ";
  cin >> n;

  cout << "Enter first number ";
  cin >> first;
  cout << "Enter second number ";
  cin >> second;


	return 0;
}
